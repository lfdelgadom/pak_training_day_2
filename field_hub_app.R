# clean the environmet 
rm(list = ls())

# install fieldhub package
install.packages("FielDHub")

# This is a basic example which shows you how to launch the app:
library(FielDHub)

# Launching the app
run_app()
